module.exports = function(db, {proffyValue, classValue, classScheduleValue}) {
    // inserir dados na tabela de teachers
    await db.run().then() //não precisa do then porque o await já presupões que ele só irá executar a próxima linha depois de concluído

    await db.run().then()
}